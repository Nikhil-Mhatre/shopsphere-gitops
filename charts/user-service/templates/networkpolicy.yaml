{{- if .Values.networkPolicy.enabled }}

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: {{ include "user-service.fullname" . }}

spec:
  podSelector: {}

  policyTypes:
    - Ingress

{{- end }}
