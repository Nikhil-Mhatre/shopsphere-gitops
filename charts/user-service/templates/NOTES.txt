Thank you for installing the User Service!

Release Name:
  {{ .Release.Name }}

Namespace:
  {{ .Release.Namespace }}

Chart:
  {{ .Chart.Name }} {{ .Chart.Version }}

Application Version:
  {{ .Chart.AppVersion }}

Service:
  {{ include "user-service.fullname" . }}

Service Type:
  {{ .Values.service.type }}

Application Port:
  {{ .Values.service.port }}

To verify the deployment:

  kubectl get pods -n {{ .Release.Namespace }}

  kubectl get svc -n {{ .Release.Namespace }}

To inspect the Deployment:

  kubectl describe deployment {{ include "user-service.fullname" . }} \
    -n {{ .Release.Namespace }}

To inspect application logs:

  kubectl logs deployment/{{ include "user-service.fullname" . }} \
    -n {{ .Release.Namespace }}

Health Endpoint:

  http://<service-ip>/health

If an Ingress or Gateway is configured, access the service using the configured hostname.
