Thank you for installing PostgreSQL!

Release Name:
  {{ .Release.Name }}

Namespace:
  {{ .Release.Namespace }}

PostgreSQL Service:
  {{ include "postgres.fullname" . }}

Port:
  {{ .Values.service.port }}

Database:
  {{ .Values.database.name }}

To connect from another application inside the cluster:

Host:
  {{ include "postgres.fullname" . }}

Port:
  {{ .Values.service.port }}

To retrieve the generated credentials (when using the chart-managed Secret):

kubectl get secret {{ include "postgres.fullname" . }} \
  -n {{ .Release.Namespace }} \
  -o jsonpath="{.data.POSTGRES_USER}" | base64 --decode

kubectl get secret {{ include "postgres.fullname" . }} \
  -n {{ .Release.Namespace }} \
  -o jsonpath="{.data.POSTGRES_PASSWORD}" | base64 --decode
